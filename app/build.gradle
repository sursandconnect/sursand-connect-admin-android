plugins {
    id 'com.android.application'
}

android {
    namespace 'in.sursandconnect.admin'
    compileSdk 35

    defaultConfig {
        applicationId 'in.sursandconnect.admin'
        minSdk 24
        targetSdk 35
        versionCode 5
        versionName '1.1.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}
